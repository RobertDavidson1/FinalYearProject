## Getting started

Make sure you have [uv](https://docs.astral.sh/uv/) installed. Then run the following commands:

```bash
git clone git@github.com:RobertDavidson1/FinalYearProject.git
cd FinalYearProject
uv sync
```
